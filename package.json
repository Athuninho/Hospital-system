{
  "name": "hospital-system",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev:api": "pnpm --filter @hospital/api-gateway dev",
    "dev:ws": "pnpm --filter @hospital/ws-server dev",
    "dev:worker": "pnpm --filter @hospital/worker dev",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "prisma": "^5.14.0"
  }
}
